fastapi~=0.115.0
uvicorn[standard]~=0.30.0
jinja2~=3.1.4
python-multipart~=0.0.9
yt-dlp>=2026.3.17
openai>=1.50.0
dashscope~=1.20.0
python-dotenv~=1.0.0
yutto~=2.0.0
gallery-dl~=1.27.0
markdown~=3.7
bleach~=6.1
# 可插拔摘要引擎：Claude 视觉/长上下文（SUMMARY_ENGINE=claude 时使用）
anthropic>=0.40.0
# 跨任务语义检索向量库（EMBEDDING_BACKEND 启用时使用）
sqlite-vec>=0.1.6
# 本地转录后端（可选，TRANSCRIBE_BACKEND=whisper 时使用）
faster-whisper~=1.0
# XHS-Downloader（小红书）：内置 bundle 的运行期依赖也须装进主 venv，
# 否则 download_xhs 一调用就 ModuleNotFoundError（emoji/lxml/aiosqlite 此前漏声明）。
rich>=14.0
pyperclip>=1.11
websockets>=12.0
rookiepy>=0.5.0
emoji>=2.14
aiosqlite>=0.20
lxml>=5.0
# 微信公众号等 HTML 图文正文抽取（platform=wechat 时使用）
trafilatura>=2.0
